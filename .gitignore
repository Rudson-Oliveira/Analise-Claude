# Segredos e ambiente — NUNCA commitar
.env
*.env
.env.*

# Dados / backups do n8n local (o docker-compose.yml fica versionado;
# .env ja e ignorado globalmente acima)
n8n-local/.env
backups/
*.sql.gz

# Configs locais do Claude Code com possiveis segredos
.mcp.local.json
.claude/settings.local.json

# SO / editores
.DS_Store
Thumbs.db
